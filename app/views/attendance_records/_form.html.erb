<%= form_with(model: attendance_record) do |form| %>

  <div class="field">
    <%= form.label :member_id %>
    <%= form.text_field :member_id %>
  </div>

  <div class="field">
    <%= form.label :event_id %>
    <%= form.text_field :event_id %>
  </div>

  <div class="field">
    <%= form.label :confirmed_attending %>
    <%= form.text_field :confirmed_attending %>
  </div>

  

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
